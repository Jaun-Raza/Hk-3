/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['next-ecommerce-template-4.vercel.app', 'plus.unsplash.com', 'images.unsplash.com'], 
    },
  };
  

export default nextConfig;
